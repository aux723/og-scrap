remove posts that are in the past
develop models for telegram and x
before adding to own db, check if link is contained in main-posted-db and main-db to ensure that only unique links are added to the db
add tag to scrap-obj eg. from: predoc-x or from: predox-tg
add posts from both collections to main-posted-db
if no new post; delete posts from main-posted-db using their tags
convert to objects within the array and store to db for tg posts; {title, body, link, deadline}
for x craft post design, check char length, if within limit, store to db, else trim/remove fields of research element if still not within limits, skip
FOR DB::
only create a new collection where both tg and x target posts will be stored when retrieving use target x and check if already contained in posted-db by checking for the property values of link and target (if target - x, and link is the same with that of an obj already stored in posted-db, get new obj)
add year added to db fields
if deadline is rolling and year added to db field is older than one year, delete post
on twitter fn, replace sponsorsing institution with sponsors and sponsoring instituion with institution